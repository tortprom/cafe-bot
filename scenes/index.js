const {callService, callServiceScene} = require('./serviceScene.js');
const {mainMenuService, mainMenuScene} = require('./mainMenu.js');
const {tableMap, tableMapScene} = require('./tableScene.js');
const {booking, bookinScene} = require('./book.js');
const {feed, feedScene} = require('./feed.js');

module.exports = {
    callService,
    callServiceScene,
    mainMenuService,
    mainMenuScene,
    tableMap,
    tableMapScene,
    booking,
    bookinScene,
    feed,
    feedScene
};
